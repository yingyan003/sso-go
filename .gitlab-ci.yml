ymltest:
 stage: test
 script:
  - echo $(dirname "$PWD")
  - PARENTDIR=$(dirname "$PWD")
  - GOPATH=$PARENTDIR"/gopath"
  - SRC=$GOPATH"/src"
  - if [ -d "$GOPATH" ]
  - then
  - echo "gopath exists"$GOPATH
  - else 
  - mkdir $GOPATH
  - fi
  - if [ -d "$SRC" ]
  - then
  - echo "src exists"$SRC
  - else
  - mkdir $SRC
  - fi
  - export GOPATH=$GOPATH
  - echo "gopath="$GOPATH
  - echo "pwd="$PWD
  - ls
  - cd ..
  - pwd
  - ls 
  - cp -r sso $SRC
  - cd $SRC
  - pwd