variables:
  IMAGE: "gitlab-ci-demo"
  DATA: "{\"image\":\"img.reg.3g:15000/$IMAGE:$CI_PIPELINE_ID\"}"
stages:
- test
#- build
#- deploy
golang_test:
  stage: test
  script:
    - PARENTFOLDER=$(dirname "$PWD")
    - echo $PARENTFOLDER
    # - GOPATH=$SSO"/gopath"
    # - GOPATHSRC=$SSO"/gopath/src"
    # - if [ -f "$GOPATH" ] ;then echo "gopath exists"; else mkdir $GOPATH ;fi
    # - if [ -f "$GOPATHSRC" ] ;then echo "gopath src exists"; else mkdir $GOPATHSRC ;fi
    # - export GOPATH=$GOPATH
    # - echo $GOPATH
    # - cp -r $SSO $GOPATHSRC
    # - cd $SSO
    # - ls -la
